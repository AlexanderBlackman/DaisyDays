<Page
    x:Class="DaisyDays.Views.SplitPicturePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DaisyDays.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ct="using:CommunityToolkit.WinUI.UI.Controls" 
    xmlns:viewmodels="using:DaisyDays.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <ItemsRepeater
            ItemsSource="{x:Bind PicturesInFolder}"
            >
            <ItemsRepeater.ItemTemplate>
                <DataTemplate >
                    <ct:ImageEx
                        Source="{Binding PicturesInFolder, Mode=OneWay}"
                        />
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
            <ItemsRepeater.Layout>
                <ct:WrapLayout
                    Orientation ="Vertical"
                    >

                </ct:WrapLayout>
            </ItemsRepeater.Layout>
        </ItemsRepeater>
    </Grid>

</Page>
